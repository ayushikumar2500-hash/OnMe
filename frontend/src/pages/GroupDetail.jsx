import React from 'react';

export default function GroupDetail({ groupId }) {
  return (
    <section className="panel card">
      <h2>Group #{groupId}</h2>
      {/* Place existing members list, expenses list/form, and balances table, scoped to groupId */}
    </section>
  );
}